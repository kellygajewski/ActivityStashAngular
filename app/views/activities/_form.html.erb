<%= bootstrap_form_for (@activity) do |f| %>
	<%= f.text_field :name %>
	<%= f.text_field :location %>
	<%= f.url_field :link %>
	<%= f.text_area :notes %>

	
  <%= f.check_boxes_collection :categories, Category.all, :id, :name %>


	<%= f.submit "Submit" %>

	<% end %>